name = "bingo"
pages_build_output_dir = ".open-next"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# KV-Binding über Cloudflare Pages Dashboard konfigurieren:
# Settings → Functions → KV namespace bindings → Variable name: BINGO_KV
# Bis dahin läuft die App mit In-Memory-Fallback (Daten gehen bei Restart verloren).
